# zero: a model built from the ground up

Not yet designed. DESIGN.md is the numbered design document; items are added
and approved one at a time before anything is built. The package will follow
the same split as `sq`: a Rust crate for play and a Python package for
training, reusing `engine`, `search` and `match`.
